package org.xydra.log;

/**
 * Marks implementing classes as thread-safe. Should only be implemented by
 * classes which are actually thread-safe.
 * 
 * @author Kaidel
 */

/*
 * TODO is there a better way for doing this? Maybe there's a standard interface
 * in the official Java library?
 */
public interface IThreadSafe {
	
}
