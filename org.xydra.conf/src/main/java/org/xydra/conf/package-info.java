/**
 * An environment that is usually instantiated only once (singleton-style) but
 * can also be created multiple times (testability with multiple parallel
 * running parts).
 */
package org.xydra.conf;