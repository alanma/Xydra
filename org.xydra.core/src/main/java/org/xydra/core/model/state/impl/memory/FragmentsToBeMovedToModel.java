package org.xydra.core.model.state.impl.memory;

// FIXME use or kill
public class FragmentsToBeMovedToModel {
	
	// private void saveModelStateRecursively(XModelState modelState) {
	// XSPI.getStateResolver().saveModelState(modelState);
	// for(XID objectStateID : modelState) {
	// XObjectState objectState = modelState.getObjectState(objectStateID);
	// XSPI.getStateResolver().saveObjectState(objectState);
	// for(XID fieldStateID : objectState) {
	// XFieldState fieldState = objectState.getFieldState(fieldStateID);
	// XSPI.getStateResolver().saveFieldState(fieldState);
	// }
	// }
	// }
	
}
